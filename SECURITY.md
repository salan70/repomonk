# セキュリティポリシー

## 対応バージョン

個人が開発している 0.x のプロジェクトです。修正は**最新リリースに対してのみ**
行います。古いバージョンへのバックポートはしません。

| バージョン | 対応 |
| --- | --- |
| 最新リリース | 対応 |
| それ以前 | 非対応（最新版へ更新してください） |

## 報告方法

**public な Issue に脆弱性の内容を書かないでください。**

GitHub の [Private vulnerability reporting](https://github.com/salan70/repomonk/security/advisories/new)
から報告してください。次の情報があると助かります。

- 再現手順と、影響を受ける `repomonk --version` の出力
- OS とターミナル
- 想定される影響

対応は**ベストエフォート**です。応答期限や修正期限は約束できません。

## 想定しているリスクの範囲

repomonk は**打鍵イベントや統計を外部へ送信しません**。データはすべてローカルに
置かれ、ネットワークを使うのはリモートリポジトリを `git clone` するときだけです。
そのため、想定する脆弱性はネットワーク越しの攻撃よりも、ローカルでの
ファイル操作とコマンド実行に寄ります。具体的には次のような領域です。

- ユーザー入力から `git` のコマンドライン引数を組み立てる箇所
  （URL の検証、オプション終端の扱い）
- リポジトリ走査時のシンボリックリンクの追跡
- clone 先および `--purge` の削除対象が、アプリの管理ディレクトリの内側に
  収まっていることの検証
- SQLite への書き込みとその破損時の挙動

repomonk が扱うのはユーザー自身が指定したリポジトリのソースコードです。
**取得したリポジトリの内容そのものは信頼できない入力として扱います**。
表示・整形の過程で任意コード実行やパス脱出につながる経路を見つけた場合は
報告対象です。

## 範囲外

- ビルド済みバイナリに**コード署名・公証を行っていない**こと。macOS や
  Windows で警告が出るのは既知かつ意図した状態です。
- 第三者が改変して再配布したバイナリ。配布物の正当性は Releases に置いた
  `SHA256SUMS` で確認してください。
- 依存クレート自体の脆弱性は、原則として上流へ報告してください。ただし
  repomonk 側で回避や更新が必要そうなら教えてもらえると助かります。
