# リポジトリ作業ルール

## 文書の優先順位

- `docs/product-requirements.md` をユーザーから見える挙動の正とする。
- `docs/architecture.md` をモジュール境界と依存方向の基準とする。
- `docs/ui-guidelines.md` をTUIの見た目・区切り・共有ヘルパーの基準とする。
  ユーザーから見える挙動は上書きしない。
- `docs/implementation-plan.md` はマイルストーンの範囲を定めるものであり、
  プロダクトの挙動を上書きしない。
- 未決事項を解決した場合は `docs/decisions.md` に記録する。

文書間に矛盾がある場合、実装都合で暗黙に決めず、プロダクト要件を保ったまま
矛盾を報告または記録する。

## 実装ルール

- 打鍵状態機械をターミナル描画やファイルI/Oから独立させる。
- リポジトリ取得、抽出、進捗、永続化、UIを別のモジュール境界に置く。
- GitHub APIへの依存を追加しない。リポジトリ取得にはシステムの`git`を使う。
- 打鍵イベントや統計を外部送信しない。関連データはすべてローカルに置く。
- refresh時の完了引き継ぎは、ファイル全体の正規化済み本文ハッシュで判断する。
- 写経単位はファイル全文とし、進捗は正規化済み行数で集計する。
- 対象外理由を内部ログではなく、ユーザーに表示するデータとして扱う。
- TUIの区切りとフッタは `docs/ui-guidelines.md` に従い、`theme` のヘルパーを使う。
- MVP外の機能を、担当エージェントの判断だけで追加しない。

## 検証ルール

- 打鍵エンジンの各状態遷移と抽出ルールには単体テストを追加する。
- 機能追加に合わせ、CLI、永続化、refresh、リポジトリ走査の統合テストを追加する。
- 画面描画はスナップショットテストを利用してよいが、挙動の検証はUIより下の
  レイヤーで行う。
- 実装タスクは、フォーマット、lint、テストがすべて通った時点で完了とする。

Rustプロジェクト初期化後の標準検証コマンドは次のとおり。

```sh
cargo fmt --all --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-features
```

## Git運用ルール

- 作業ブランチを切らず、`main`に直接コミットする。
- ユーザーがコミットを指示した場合、確認を挟まずそのまま`origin/main`へ
  プッシュする。
- コミットメッセージはConventional Commits形式、本文は日本語で書く。
- **コミットは「後から個別にrevertできる単位」で分割する。** ユーザーからの
  指示を待たず、既定の作法としてそうする。1つの作業依頼が複数の関心事に
  またがるなら、依頼が1つでもコミットは複数に割る。判断基準は次のとおり。
  - 片方だけを取り消したくなる可能性があるものは、別のコミットにする。
  - 依存関係のある変更は、参照される側を先にコミットする。各コミットの時点で
    リポジトリが壊れていない順序にする。
  - 機械的な整形やリネームは、挙動を変える修正と混ぜない。
  - 逆に、単体では意味をなさず必ず一緒に戻すものは、無理に分けない。
    実装とそのテストは同じコミットに入れる。
- コミット本文には、何をしたかだけでなく**なぜそうしたか**を書く。差分を読めば
  分かることではなく、差分からは読み取れない判断の理由を残す。

## リリース運用ルール

- リリースは`vX.Y.Z`タグのpushによってのみ行う。タグ名は`Cargo.toml`の
  `version`と一致させる。`repomonk --version`は`CARGO_PKG_VERSION`由来のため、
  ズレると配布物が自分のバージョンについて嘘をつく。
- **エージェントはユーザーの明示指示なしにタグをpushしない。** 通常のコミットは
  従来どおり確認なしで`origin/main`へpushしてよいが、タグはビルド済みバイナリを
  外部へ公開する操作なので区別する。
- 一度pushしたタグは打ち直さず、削除もしない。誤った場合は次のパッチ版を出す。
  すでに`install.sh`経由で入れた利用者がいる前提で考える。
- 0.xの間は、ユーザーから見える挙動の破壊的変更でminor、それ以外でpatchを上げる。
- `CHANGELOG.md`は持たない。リリースノートは`gh release --generate-notes`が
  コミット一覧から生成するため、Conventional Commitsの遵守がそのまま
  リリースノートの品質になる。
- `main`にbranch protectionはかけない。品質ゲートは
  `.github/workflows/release.yml`の`verify`ジョブに置き、赤いままリリースが
  出ていかないようにする。CI（`.github/workflows/ci.yml`）は通知装置として扱う。
- 依存クレートを増減したら`THIRD-PARTY-LICENSES.md`を再生成する。手順は
  `docs/development.md`を参照。

## AIエージェントによる実装

- 実装単位はリリース段階ではなく、`docs/implementation-plan.md`に定める
  並列ワークストリームとする。
- 各担当は公開型、エラー型、境界traitなどの契約を先に合意し、担当外の内部へ
  依存しない。
- 同じファイルを複数担当が同時に編集しない。共有ファイルの変更は統合担当が行う。
- 各担当はコード、単体テスト、担当範囲の説明をまとめて完了させる。
- 統合担当は全機能を結線し、E2Eシナリオと品質ゲートが通るまでMVPを完了扱いに
  しない。
- SQLite、ファイルツリー、clone、進捗保存はMVPに含む。依存順、詳細統計、演出は
  MVP外とする。
